"use client";

/**
 * Regulatory / market headline strip for the AI Workspace.
 * Hidden for SME-focused workspace; re-enable later for enterprise/NGO tenants (e.g. gate on orgType).
 */
export function ExternalIntelligenceWorkspaceStrip() {
  return null;
}
